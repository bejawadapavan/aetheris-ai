# Base URL of the backend API. During local dev, Vite proxies /api to
# http://localhost:5000 automatically (see vite.config.js), so this can be
# left as-is. Set it explicitly for production deployments.
VITE_API_URL=/api
